# Create an identity matrix with np.eye(4) and a random matrix with np.random.randint(1, 100, size=(3,3)).

import numpy as np

arr = np.eye(4)
print(arr)

# Random matrix
kk = np.random.randint(1,100, size =(3,3))
print(kk)